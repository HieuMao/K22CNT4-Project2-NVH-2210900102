@model IEnumerable<K22CNT4_TTCD_Nguyễn_Văn_Hiếu_2210900102.Models.QUAN_TRI>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Tài khoản quản trị</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.admin_pass)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.admin_status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.admin_pass)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.admin_status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.admin_username }) |
            @Html.ActionLink("Details", "Details", new { id=item.admin_username }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.admin_username })
        </td>
    </tr>
}

</table>
